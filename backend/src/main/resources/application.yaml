spring:
  datasource:
    url: jdbc:postgresql://localhost/findmycar
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create